<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/header.ejs') %>
    <main>
        <div class="container">
            <h2>Welcome, <%= locals.user.name %>!</h2>
            <h3>Available Carpool Offers</h3>
            <a href="/create-offer" class="button">Create a New Offer</a>
            <div class="carpool-list">
                <% if (carpools.length > 0) { %>
                    <% carpools.forEach(carpool => { %>
                        <div class="carpool-item">
                            <h4><%= carpool.carName %></h4>
                            <p>From: <%= carpool.location %></p>
                            <p>Time: <%= carpool.time %></p>
                            <p>Price: Rs.<%= carpool.price %></p>
                            <p>Gender: <%= carpool.gender %></p>
                            <% if (carpool.genderPreference) { %>
                                <p>Open for: Girls only</p>
                            <% } %>
                            <p>Availability: <%= carpool.bookedSeats %>/<%= carpool.totalSeats %></p>
                            <p>Posted by: <%= carpool.userId.name %></p>
                            
                            <% if (!carpool.userId.equals(locals.user.id)) { %>
                                <form action="/carpools/<%= carpool._id %>/book" method="POST" style="display:inline;">
                                    <% if (carpool.bookedSeats < carpool.totalSeats) { %>
                                        <button type="submit" class="button book-button">Book Carpool</button>
                                    <% } else { %>
                                        <button type="submit" class="button book-button" disabled>Fully Booked</button>
                                    <% } %>
                                </form>
                            <% } %>
            
                            <% if (carpool.userId.equals(locals.user.id)) { %>
                                <form action="/carpools/<%= carpool._id %>/delete?_method=DELETE" method="POST" style="display:inline;">
                                    <button type="submit" class="button delete-button">Delete Your Offer</button>
                                </form>
                            <% } %>
                            
                            <a href="/chat/<%= carpool._id %>" class="button">Chat with Uploader</a>
                        </div>
                    <% }); %>
                <% } else { %>
                    <p>No carpool offers available at the moment.</p>
                <% } %>
            </div>
        </div>
    </main>
    <%- include('../partials/footer.ejs') %>
</body>
</html>